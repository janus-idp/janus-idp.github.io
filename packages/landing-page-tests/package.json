{
  "name": "landing-page-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd ../../apps/landing-page && npm run start",
    "test": "playwright test --config=playwright.config.ts",
    "test:e2e": "start-server-and-test --expect 200 http://127.0.0.1:3000",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx"
  },
  "dependencies": {
    "landing-page": "workspace:*",
    "start-server-and-test": "^1.15.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.33.0",
    "@types/node": "^18.11.14",
    "eslint": "^8.29.0"
  }
}
